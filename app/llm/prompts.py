THINK_PROMPT_QWEN = """
<|im_start|>system
You are an algorithm analysis assistant.

Analyze the given Korean coding problem.

Output:

## Problem Understanding
## Algorithm Selection
## Selection Reason
## Implementation Plan

Use English.
Do not write code.
Use concise and structured explanations.
<|im_end|>
<|im_start|>user
{problem}
<|im_end|>
<|im_start|>assistant

"""


THINK_PROMPT = """
당신은 알고리즘 코치이다.

아래 문제를 분석하여 사고과정을 작성하라.

분석 시 다음을 고려하라:
- 제약조건(입력 크기, 값의 범위)을 먼저 확인하고 허용 가능한 시간복잡도를 추정할 것
- 단순하지만 비효율적인 접근법을 먼저 떠올리고, 왜 최적이 아닌지 설명할 것
- 그 다음 더 효율적인 접근법을 도출하고, 왜 그것이 동작하는지 논리적으로 설명할 것
- 엣지 케이스(빈 입력, 최소/최대 경계값, 중복값 등)를 고려할 것

다음 형식으로 작성하라.

1. 문제 요약
2. 제약조건 분석 (입력 크기, 시간복잡도 한계 추정)
3. 후보 접근법과 비효율성 (떠올렸지만 채택하지 않은 방법과 그 이유)
4. 핵심 아이디어
5. 사용할 알고리즘 / 자료구조
6. 시간복잡도 및 공간복잡도
7. 구현 포인트 및 주의할 엣지 케이스

문제:
{problem}
"""


CODE_GENERATION_PROMPT = """
당신은 파이썬 알고리즘 전문가이다.

문제와 사고과정을 참고하여 정답 코드를 작성하라.

목표:
생성된 코드는 실제 온라인 저지(백준 등)에서 실행 가능한 형태여야 하며,
표준 입력(stdin)으로 데이터를 읽고 표준 출력(stdout)으로 결과를 출력해야 한다.
함수를 정의하고 코드 내부에서 직접 호출하여 테스트하는 방식(예: print(solution(1, 2, 3)) 과 같이 하드코딩된 실행)은 절대 허용되지 않는다.

필수 코드 구조:

1. 코드는 input() 또는 sys.stdin을 통해 모든 값을 읽어야 한다.
   - 정수/문자열 한 줄: x = input()
   - 공백으로 구분된 여러 값: a, b = map(int, input().split())
   - 리스트 형태의 여러 줄 입력: 몇 개의 줄을 읽을지 먼저 입력받거나 문제 명세에 따라 반복문으로 처리
2. 입력으로 받은 값을 가지고 알고리즘 로직을 수행한다.
   (필요하다면 내부적으로 함수를 정의해 호출하는 것은 허용되나, 입력 자체는 반드시 stdin에서 와야 한다.)
3. 최종 결과는 print()로 출력한다. return으로 끝나면 안 된다.
4. 코드는 입력을 어떻게 파싱할지 스스로 결정해야 하며, 그 파싱 형식이 test_input의 형식과 정확히 일치해야 한다.

test_input 작성 규칙:

* test_input은 실제로 코드의 input()/sys.stdin에 한 줄씩 또는 여러 줄로 주입될 "텍스트"이다.
* 여러 줄 입력이 필요하면 \\n으로 줄을 구분한 하나의 문자열로 작성하라.
* 문제에 명시된 예제 입출력이 있다면 반드시 그 형식을 그대로 따라 test_input을 구성할 것.
* 문제에 예제가 없다면 제약조건을 만족하는 가장 간단한 예제를 직접 생성할 것.

검증 절차 (반드시 스스로 시뮬레이션할 것):

1. 작성한 코드를 머릿속에서 한 줄씩 실행하며, test_input의 각 줄이 코드의 몇 번째 input() 호출에 어떤 값으로 들어가는지 정확히 추적하라.
2. 그 결과로 출력되는 값이 expected_output과 정확히 일치하는지 확인하라.
3. 일치하지 않으면 코드 또는 test_input을 수정하여 다시 1번부터 검증하라.
4. 코드에 input() 호출이 없거나 print()로 끝나지 않으면 그 자체로 실패이므로 반드시 수정하라.

요구사항 요약:

* 사고과정에서 도출된 알고리즘과 일치하는 코드를 작성할 것
* 반드시 Python 코드만 작성할 것
* 반드시 input() 또는 sys.stdin으로 입력을 받을 것 (함수 인자로 테스트값을 직접 넘기지 말 것)
* 반드시 print()로 결과를 출력할 것
* 함수 정의만 하고 끝내지 말고, 실행 가능한 완전한 프로그램을 작성할 것
* 가독성 있는 변수명과 최소한의 주석을 사용할 것
* test_input을 코드에 입력했을 때 expected_output이 정확히, 한 글자도 다르지 않게 출력되어야 한다

반드시 아래 JSON 형식으로만 응답하라.
다른 설명, 마크다운, 코드블록은 절대 포함하지 마라.

{{
"code": "python code (stdin/stdout 기반 완전한 프로그램)",
"test_input": "코드에 그대로 주입 가능한 입력 텍스트 (여러 줄은 \\n으로 구분)",
"expected_output": "sample output"
}}

문제:
{problem}

사고과정:
{think_process}
"""



DEBUG_PROMPT = """
당신은 파이썬 디버깅 전문가이다.

아래 정보를 바탕으로 코드의 오류를 분석하고 수정하라.

문제:
{problem}

현재 코드:
{code}

입력:
{test_input}

기대 출력:
{expected_output}

실제 출력:
{actual_output}

분석 시 다음을 따르라:
- 기대 출력과 실제 출력의 차이를 구체적으로 비교할 것
- 오류가 로직 오류, 엣지 케이스 누락, 자료구조 오용, 인덱스/타입 오류 중 어디에 해당하는지 분류할 것
- 수정된 코드는 기존 코드의 구조와 의도를 최대한 유지하면서 최소한으로 수정할 것
- 수정 후 동일한 입력에 대해 기대 출력과 일치하는지 다시 검증할 것

반드시 아래 JSON 형식으로만 응답하라. 다른 설명이나 마크다운은 포함하지 마라.

{{
  "reason": "원인",
  "fixed_code": "수정된 코드"
}}
"""


QUERY_PROMPT = """
너는 코딩테스트 학습용 Query Generator다.

입력:

* 원본 문제
* 사고과정
* 생성된 코드

목표:
문제를 다시 푸는 것이 아니라,
학습자가 추가로 공부해야 할 핵심 알고리즘 또는 프로그래밍 문법을 추론하여
RAG 검색에 사용할 학습용 Query를 생성하라.

규칙:

* 원본 문제, 사고과정, 생성된 코드를 함께 분석한다.
* 사고과정에 사용된 알고리즘을 우선적으로 선택한다.
* 알고리즘이 명확하지 않으면 코드에 사용된 자료구조 또는 문법을 선택한다.
* 문제 자체가 아닌 학습 개념을 검색하는 Query를 생성한다.
* Query는 자연어 질문 형태로 작성한다.
* 문제 번호, 플랫폼명, 정답 코드, 테스트케이스는 포함하지 않는다.
* 반드시 하나의 Query만 생성한다.

출력:
알고리즘, 프로그래밍 언어 문법, 구현 방안 등의 자연어 질문

입력:

[원본 문제]
{problem}

[사고과정]
{think_process}

[생성된 코드]
{code}
"""


FINAL_PROMPT = """
당신은 알고리즘 강사이다.

아래 자료를 바탕으로 학습자를 위한 풀이 설명을 작성하라.

문제:
{problem}

사고과정:
{think_process}

개념자료:
{concept_docs}

정답코드:
{code}

작성 시 다음을 따르라:
- "핵심 알고리즘 설명"은 개념자료에 제시된 용어, 정의 순서, 설명 방식을 최대한 따를 것 (개념자료의 표현 스타일을 그대로 차용)
- 코드 설명은 코드의 각 블록이 사고과정의 어느 단계에 대응하는지 연결지어 설명할 것
- 시간복잡도는 결과만 제시하지 말고 어떤 연산이 비용을 발생시키는지 근거를 들어 설명할 것

다음을 작성하라.

1. 문제 접근법
2. 핵심 알고리즘 설명 (개념자료의 형식을 따를 것)
3. 코드 설명
4. 시간복잡도
5. 최종 정답 코드
"""